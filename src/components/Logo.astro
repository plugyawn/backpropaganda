---
import { withBase } from "../lib/paths";

interface Props {
  compact?: boolean;
}

const { compact = false } = Astro.props;
---

<a class:list={["logo", { "logo--compact": compact }]} href={withBase("/")} aria-label="Backpropaganda home">
  <span class="logo-text">Backpropaganda</span>
</a>
